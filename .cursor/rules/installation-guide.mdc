---
description:
globs:
alwaysApply: false
---
# Installation Guide

This guide provides step-by-step instructions for setting up LocalCA-Go.

## Prerequisites

- Go 1.20+ for backend development
- Node.js 18+ and pnpm for frontend development
- Docker and Docker Compose for containerized deployment

## Local Development Setup

1. Clone the repository:
   ```bash
   git clone https://github.com/yourusername/localca-go.git
   cd localca-go
   ```

2. Create a CA key password file:
   ```bash
   echo "your-secure-password" > cakey.txt
   ```

3. Start the backend:
   - Windows: `run-dev.bat`
   - Linux/macOS: `./run-dev.sh`

4. Install frontend dependencies:
   ```bash
   pnpm install
   ```

5. Start the frontend development server:
   ```bash
   pnpm dev
   ```

6. Access the web interface at http://localhost:3000

## Docker Deployment

1. Clone the repository:
   ```bash
   git clone https://github.com/yourusername/localca-go.git
   cd localca-go
   ```

2. Create a CA key password file:
   ```bash
   echo "your-secure-password" > cakey.txt
   ```

3. Start the application with Docker Compose:
   ```bash
   docker-compose up -d
   ```

4. Access the web interface at http://localhost:3000

## Configuration

See [DEPLOYMENT.md](mdc:DEPLOYMENT.md) for detailed configuration options and environment variables.

## Development Scripts

- `pnpm dev` - Start Next.js development server
- `pnpm build` - Build the Next.js application
- `pnpm start` - Start the production Next.js server
- `go run main.go` - Run the Go backend
